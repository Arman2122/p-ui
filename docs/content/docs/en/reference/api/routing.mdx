---
title: Routing
description: "Operator routing intent: which inbounds, which criteria, which
  destination. A rule names inbounds by ID rather than by tag, so renaming or
  deleting an inbound cannot silently widen it to every inbound on the box. The
  compile decides which mechanism realises each rule -- an Xray inbound is named
  directly, while a kernel inbound is fronted and its rule rewritten onto the
  front's tag -- which is what lets one screen route any protocol to any
  protocol. A rule naming an inbound the router cannot see is refused at save
  rather than accepted and left to never match. All endpoints under
  /panel/api/routing."
full: true
_openapi:
  preload:
    - ./public/openapi.json
  toc:
    - depth: 2
      title: List every routing rule in the operator's own first-match order.
      url: '#list-every-routing-rule-in-the-operators-own-first-match-order'
    - depth: 2
      title: Create a rule. It is appended last and converges before the response
        returns.
      url: '#create-a-rule-it-is-appended-last-and-converges-before-the-response-returns'
    - depth: 2
      title: Which inbounds a rule may name. An inbound that cannot be a subject right
        now is listed with routable=false and a blockedKey naming why, never
        omitted. criteriaMask names the criteria that can actually match on it
        -- user is absent for a kernel ingress because Xray's tun handler builds
        a user with no email, so the matcher can never return true.
      url: '#which-inbounds-a-rule-may-name-an-inbound-that-cannot-be-a-subject-right-now-is-listed-with-routablefalse-and-a-blockedkey-naming-why-never-omitted-criteriamask-names-the-criteria-that-can-actually-match-on-it----user-is-absent-for-a-kernel-ingress-because-xrays-tun-handler-builds-a-user-with-no-email-so-the-matcher-can-never-return-true'
    - depth: 2
      title: "Which uplinks a rule may be pointed at. Operator-owned exits only -- a
        panel-owned front is machinery for an ingress and never a destination.
        The handle that realises an exit is deliberately absent: which device or
        port carries it is the compile's business, and an editor that showed it
        would invite an operator to depend on it."
      url: '#which-uplinks-a-rule-may-be-pointed-at-operator-owned-exits-only----a-panel-owned-front-is-machinery-for-an-ingress-and-never-a-destination-the-handle-that-realises-an-exit-is-deliberately-absent-which-device-or-port-carries-it-is-the-compiles-business-and-an-editor-that-showed-it-would-invite-an-operator-to-depend-on-it'
    - depth: 2
      title: Set the whole first-match order in one write. The body is the complete id
        list; a partial application would silently change which rule wins.
      url: '#set-the-whole-first-match-order-in-one-write-the-body-is-the-complete-id-list-a-partial-application-would-silently-change-which-rule-wins'
    - depth: 2
      title: Update one rule.
      url: '#update-one-rule'
    - depth: 2
      title: Delete one rule and converge.
      url: '#delete-one-rule-and-converge'
  structuredData:
    headings:
      - content: List every routing rule in the operator's own first-match order.
        id: list-every-routing-rule-in-the-operators-own-first-match-order
      - content: Create a rule. It is appended last and converges before the response
          returns.
        id: create-a-rule-it-is-appended-last-and-converges-before-the-response-returns
      - content: Which inbounds a rule may name. An inbound that cannot be a subject
          right now is listed with routable=false and a blockedKey naming why,
          never omitted. criteriaMask names the criteria that can actually match
          on it -- user is absent for a kernel ingress because Xray's tun
          handler builds a user with no email, so the matcher can never return
          true.
        id: which-inbounds-a-rule-may-name-an-inbound-that-cannot-be-a-subject-right-now-is-listed-with-routablefalse-and-a-blockedkey-naming-why-never-omitted-criteriamask-names-the-criteria-that-can-actually-match-on-it----user-is-absent-for-a-kernel-ingress-because-xrays-tun-handler-builds-a-user-with-no-email-so-the-matcher-can-never-return-true
      - content: "Which uplinks a rule may be pointed at. Operator-owned exits only -- a
          panel-owned front is machinery for an ingress and never a destination.
          The handle that realises an exit is deliberately absent: which device
          or port carries it is the compile's business, and an editor that
          showed it would invite an operator to depend on it."
        id: which-uplinks-a-rule-may-be-pointed-at-operator-owned-exits-only----a-panel-owned-front-is-machinery-for-an-ingress-and-never-a-destination-the-handle-that-realises-an-exit-is-deliberately-absent-which-device-or-port-carries-it-is-the-compiles-business-and-an-editor-that-showed-it-would-invite-an-operator-to-depend-on-it
      - content: Set the whole first-match order in one write. The body is the complete
          id list; a partial application would silently change which rule wins.
        id: set-the-whole-first-match-order-in-one-write-the-body-is-the-complete-id-list-a-partial-application-would-silently-change-which-rule-wins
      - content: Update one rule.
        id: update-one-rule
      - content: Delete one rule and converge.
        id: delete-one-rule-and-converge
    contents: []
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="./public/openapi.json" webhooks={[]} operations={[{"path":"/panel/api/routing/rules","method":"get"},{"path":"/panel/api/routing/rules","method":"post"},{"path":"/panel/api/routing/subjects","method":"get"},{"path":"/panel/api/routing/exits","method":"get"},{"path":"/panel/api/routing/rules/order","method":"post"},{"path":"/panel/api/routing/rules/{id}","method":"post"},{"path":"/panel/api/routing/rules/{id}/del","method":"post"}]} showTitle />
    </>
  );
}